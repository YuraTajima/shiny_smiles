<div class="container">
   
    <div class="col-12">
      
        <div class="mt-3">
          <h3 class="text-secondary"><strong>ジャンル一覧・追加</strong></h3>
        </div>
        
        <div class="container pb-3 mx-5">
            
            <!--<div class="text-center">-->
            <!--    %= form_with model: @genre, url: admin_genres_path, method: :post do |f| %>-->
            <!--    <p>ジャンル名</p>-->
            <!--    <p>%= f.text_field :name %></p>-->
            <!--    <p>%= f.submit '新規登録' %></p>-->
            <!--    % end %>-->
            <!--</div>-->
            
            
            <div>
                <table class="table">
                <thead>
                    <tr>
                        <th class="text-center">ジャンル</th>
                        <th colspan="1"></th>
                    </tr>
                </thead>
                
                <tobody>
                    <% @genres.each do |genre| %>
                    <tr>
                        <th class="text-center"><%= genre.name %></th>
                        <th>
                            <div>
                            <%= link_to edit_admin_genre_path(genre.id) do %>
                                <button type="button" class="btn btn-success">編集する</button>
                            <% end %>
                            </div>
                        </th>
                    </tr>
                    <% end %>
                </tobody>
                </table>
            </div>
            
        </div>
    
        
    </div>
    
</div>
